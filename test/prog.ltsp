(define *main*
  (lambda (_)
    (let ((a (read-num))
          (b (read-num)))
      (cond ((less a b)
             (do (print a) (print " ") (println b)))
            (T
             (do (print b) (print " ") (println a)))))))

(dump)

